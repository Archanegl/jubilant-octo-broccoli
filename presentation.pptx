# PowerPoint (PPTX) - Slide Content

说明：此文件名为 presentation.pptx，但内容为 PPT 幻灯片的文本草稿（Markdown 格式），包含每张幻灯片的标题、要点与演讲备注。

你可以：
- 直接下载此文件并用它作为制作 PPT 的脚本或参考；
- 或者回复让我生成真正的二进制 .pptx（我会替换此文件）。

---

幻灯片 1 — 标题页
- 标题：电磁波与电磁场 — 电磁波反射与折射的应用研究
- 副标题：从经典理论到光纤、雷达与前沿器件
- 作者：依据 README 汇总
- 日期：2026年7月（可改）
备注：使用封面图（电磁波示意图或光纤/雷达图片），加上演讲者信息。

幻灯片 2 — 摘要与关键词
- 摘要要点：
  - 反射与折射是麦克斯韦方程在介质边界上的核心现象
  - 文章采用“理论简明、应用丰富”的结构，覆盖光纤、雷达、前沿电磁材料与 RIS/ISAC
- 关键词：电磁波反射与折射；光纤通信；雷达遥感；菲涅尔公式；全反射
备注：一两句话口头扩展摘要背景与目的。

幻灯片 3 — 目录（PPT 导览）
- 1 引言
- 2 基础理论概述
- 3 光纤通信
- 4 雷达与遥感
- 5 前沿技术
- 6 结论与参考文献
备注：演讲时快速过一遍结构，告知听众总时长与每部分重点。

幻灯片 4 — 引言
- 要点：
  - 反射与折射为最早认识的光学现象之一
  - 随麦克斯韦理论形成，现为电磁工程基础
  - 本文强调工程应用案例补充传统教材的理论
备注：可提及历史示例（铜镜、透镜）并过渡到现代应用。

幻灯片 5 — 基础理论概览（核心定律）
- 要点（表格/并列）：
  - 反射定律：入射角 = 反射角（θ1 = θ1'）
  - 斯涅尔定律：n1 sin θ1 = n2 sin θ2
  - 折射率定义：n = c / v = √(εr μr)
备注：建议用一张小表格显示“现象 — 公式 — 意义”。

幻灯片 6 — 菲涅尔公式核心结论
- 要点（公式展示）：
  - s 偏振（TE）：r_s = (n1 cosθ1 − n2 cosθ2)/(n1 cosθ1 + n2 cosθ2)，t_s = 2 n1 cosθ1/(n1 cosθ1 + n2 cosθ2)
  - p 偏振（TM）：r_p = (n2 cosθ1 − n1 cosθ2)/(n2 cosθ1 + n1 cosθ2)，t_p = 2 n1 cosθ1/(n2 cosθ1 + n1 cosθ2)
  - 能量守恒：R + T = 1（R = |r|^2，T = (n2 cosθ2 / n1 cosθ1) |t|^2）
备注：展示公式并配图说明 s/p 偏振方向；可配 Fresnel 曲线示意图。

幻灯片 7 — 其他重要现象（速览）
- 要点：
  - 布鲁斯特角：tan θ_B = n2 / n1（p 偏振无反射）
  - 全反射：临界角 sin θ_c = n2 / n1（n1 > n2）
  - 趋肤效应：δ = 1/√(π f μ σ)
  - 双折射与负折射（超材料）
备注：每项给出一句工程应用（偏振片、光纤、导体高频传输、隐身/平面透镜）。

幻灯片 8 — 应用实例一：光纤通信 — 概览
- 要点：
  - 光纤以全内反射为导光机制，是信息高速公路的核心
  - 探讨 NA、V 参数、损耗、色散与实际应用（海底光缆、FTTH）
备注：配合光纤剖面示意图（芯/包层/入射角）。

幻灯片 9 — 全内反射与数值孔径（NA）
- 要点：
  - 临界角：sin θ_c = n2 / n1
  - 数值孔径：NA = sin θ0,max = √(n1^2 − n2^2)
  - 物理意义：NA 越大耦合越容易，但色散与模数也可能增大
备注：配图示意接受光束进入纤芯的最大角度；列出典型 NA：单模 ~0.1–0.15，多模 ~0.2–0.3。

幻灯片 10 — 单模与多模光纤（V 参数）
- 要点：
  - V = (2π a / λ) · NA
  - 单模条件：V < 2.405 → 仅 LP01 基模
  - 设计要点：纤芯半径 a 与工作波长 λ 的关系
备注：用图示说明多模引发模式色散，单模有更高传输带宽与更细纤芯的工程注意事项。

幻灯片 11 — 光纤损耗与低损耗窗口
- 要点：
  - 损耗来源：吸收、瑞利散射（α_R ∝ 1/λ^4）
  - 三个低损耗窗口：
    - 850 nm（~2.5 dB/km）短距多模
    - 1310 nm（~0.4 dB/km）中距单模
    - 1550 nm（~0.2 dB/km）长距骨干与海缆
备注：展示一张损耗随波长变化的曲线图与对应应用场景（海缆 → 1550 nm）。

幻灯片 12 — 色散与通信容量
- 要点：
  - 色散导致脉冲展宽：Δτ = |D| · L · Δλ（D：色散系数）
  - 色散类型：材料色散、波导色散、模式色散
  - 工程影响：限制链路速率与最大传输距离，需色散补偿
备注：举例说明宽带增长（10M→100M→1G→10G）受色散限制的现实意义。

幻灯片 13 — 光纤的生活化应用
- 要点：
  - FTTH（光纤到户）
  - 内窥镜（光纤成像、照明）
  - 光纤传感（桥梁/轨道/温度/应变）
备注：配照片：光猫/光纤接头、内窥镜、桥梁光纤传感示意。

幻灯片 14 — 应用实例二：雷达与遥感 — 概览
- 要点：
  - 雷达/遥感以发射并接收回波分析目标特性为核心
  - 包含测距、测速、多普勒、雷达方程、SAR、LiDAR 等
备注：配雷达和 SAR 卫星示意图。

幻灯片 15 — 雷达测距与测速（多普勒）
- 要点：
  - 测距：R = ½ c t（t：往返时间）
  - 距离分辨率：ΔR = ½ c τ（τ：脉冲宽度）
  - 多普勒频移：f_d = 2 v_r / λ（测径向速度）
备注：举例：交通测速、救护车多普勒类比说明。

幻灯片 16 — 雷达方程与 SAR / LiDAR
- 要点：
  - 雷达回波：P_r = P_t G^2 λ^2 σ / (4π)^3 R^4
  - 最大探测距离 R_max ∝ (P_t G^2 λ^2 σ / S_min)^(1/4)
  - SAR：合成孔径提高方位分辨率，与距离无关（示意 Δa = D/2）
  - LiDAR：对扩展朗伯体目标，接收功率 ∝ 1/R^2（而非 1/R^4）
备注：强调工程含义（发射功率与探测距离的关系，SAR 在全天候成像的优势）。

幻灯片 17 — 雷达的生活化应用
- 要点：
  - 气象雷达（降水成像）
  - 汽车雷达与门禁感应
  - 无线充电中微波/毫米波聚焦（相控阵）
备注：配图：汽车雷达、气象图、感应门示例。

幻灯片 18 — 前沿技术概述（超材料 / RIS / ISAC）
- 要点：
  - 超材料 / 超表面：通过亚波长结构实现负折射、相位/振幅/偏振调控
  - 可重构智能表面（RIS）：大量可控反射单元，用于 6G 辅助通信与环境“可编程化”
  - 通信与感知一体化（ISAC）：RIS 可同时辅助通信与环境感知（呼吸/姿态监测等）
备注：建议用示意图（超材料单元/RIS 面板）并列出应用前景与挑战（可控性、能耗、标准化）。

幻灯片 19 — 结论
- 要点：
  - 反射与折射既是基础物理，也是众多工程技术的核心
  - 从全内反射的光纤到 SAR、LiDAR，再到超材料与 RIS，应用广泛且持续演进
  - 未来趋势：可重构电磁环境、感知融合与更高频段通信
备注：总结时呼应“理论简明、应用丰富”的写作思路，提出可能的研究/工程方向。

幻灯片 20 — 参考文献（选择列出）
- 主要参考（可按 README 中顺序列出）：
  - 光纤通信原理与技术（电子工业出版社，2020）
  - 谢处方、邱文杰，《电磁场与电磁波》（高教，2019）
  - 丁鹭飞、耿富录，《雷达原理》（西安电子科大出版社，2020）
  - 等（README 中的参考 1–9）
备注：在 PPT 中可只列出关键几项，并在附录页给出完整文献列表。

附录（可选幻灯片）
- 公式推导要点（如菲涅尔公式简要推导要点或边界条件）
- 符号说明表（n, θ, r, t, R, T, NA, V, D 等）
备注：便于技术听众快速参考。

---

创建信息：
- 文件名：presentation.pptx
- 路径：仓库根目录
- 提交信息：Add presentation draft (Markdown content inside PPTX-named file)

说明：如果你确认我应该把该文本替换为真正的二进制 .pptx（每页包含标题、要点和备注，并嵌入推荐图片占位符），请回复“请生成真实 PPTX”，我将生成并更新该文件。